name: tag-filter
on:
    push:
        tags:
            - 'v[0-9]+.[0-9]+.[0-9]+' # ok : v1.0.0 | v2.2.2, ng : v1.0 | 1.0.0

jobs:
    tag-filter:
        runs-on: ubuntu-latest
        steps:
            - name: echo
              run: echo hello